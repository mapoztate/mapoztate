#include <iostream>

int main() {
    std::cout << "This is purely for aesthetic purposes; my repository will be marked as having a language after putting this little piece of code.";
    return 0;
}
